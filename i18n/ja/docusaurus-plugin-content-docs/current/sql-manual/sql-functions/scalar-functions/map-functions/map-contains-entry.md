---
{
  "title": "MAP_CONTAINS_ENTRY",
  "language": "ja",
  "description": "指定されたマップが特定のエントリ（キー、値）を含むかどうかを判定します"
}
---
## 説明

指定された`map`が特定のエントリ`(key, value)`を含んでいるかどうかを判定します

## 構文

```sql
MAP_CONTAINS_ENTRY(<map>, <key>, <value>)
```
## パラメータ

| Parameter | Description |
| -- | -- |
| `<map>` | 入力するマップのコンテンツ |
| `<key>` | 取得するキー |
| `<value>` | 取得する値 |

## 戻り値

指定された`map`に特定の`(key, value)`ペアが含まれているかどうかを判定します。存在する場合は1を返し、そうでなければ0を返します。`<map>`が`NULL`の場合は、`NULL`を返します。

キーと値の比較では「null-safe equal」（2つの`NULL`は等しいとみなされる）を使用し、これは標準的なSQL定義とは異なります。

## 例

```sql
select map_contains_entry(map(null, 1, 2, null), null, 1);
```
```text
+----------------------------------------------------+
| map_contains_entry(map(null, 1, 2, null), null, 1) |
+----------------------------------------------------+
|                                                  1 |
+----------------------------------------------------+
```
```sql
select map_contains_entry(map(1, '100', 0.1, '2'), 1, '100');
```
```text
+-------------------------------------------------------+
| map_contains_entry(map(1, '100', 0.1, '2'), 1, '100') |
+-------------------------------------------------------+
|                                                     1 |
+-------------------------------------------------------+
```
```sql
select map_contains_entry(map(1, '100', 0.1, '2'), 0.11, '2');
```
```text
+--------------------------------------------------------+
| map_contains_entry(map(1, '100', 0.1, '2'), 0.11, '2') |
+--------------------------------------------------------+
|                                                      0 |
+--------------------------------------------------------+
```
