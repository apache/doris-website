---
{
  "title": "ASCII",
  "language": "ja",
  "description": "文字列の最初の文字のASCIIコード値を返します。この関数は文字列の最初の文字のみを処理します。"
}
---
## 説明

文字列の最初の文字のASCIIコード値を返します。この関数は文字列の最初の文字のみを処理し、複数文字の文字列に対しては最初の文字のASCII値のみを返します。

## 構文

```sql
ASCII(<str>)
```
## パラメータ

| パラメータ | 説明                      |
|---------|-----------------------------|
| `<str>` | 最初の文字のASCIIコードを取得する文字列。型: VARCHAR |

## 戻り値

INT型を返し、文字列の最初の文字のASCIIコード値を表します。

特殊なケース:
- パラメータがNULLの場合、NULLを返します
- 文字列が空の場合、0を返します
- 最初の文字がASCII文字でない場合（コード値が127より大きい）、対応するバイト値を返します
- マルチバイトUTF-8文字の場合、最初のバイトの値を返します

## 例

1. 基本的な数値文字

```sql
SELECT ASCII('1'), ASCII('234');
```
```text
+------------+--------------+
| ASCII('1') | ASCII('234') |
+------------+--------------+
|         49 |           50 |
+------------+--------------+
```
2. 文字

```sql
SELECT ASCII('A'), ASCII('a'), ASCII('Z');
```
```text
+------------+------------+------------+
| ASCII('A') | ASCII('a') | ASCII('Z') |
+------------+------------+------------+
|         65 |         97 |         90 |
+------------+------------+------------+
```
3. 空文字列の処理

```sql
SELECT ASCII('');
```
```text
+-----------+
| ASCII('') |
+-----------+
|         0 |
+-----------+
```
4. NULL値の処理

```sql
SELECT ASCII(NULL);
```
```text
+-------------+
| ASCII(NULL) |
+-------------+
|        NULL |
+-------------+
```
5. 特殊記号

```sql
SELECT ASCII(' '), ASCII('!'), ASCII('@');
```
```text
+------------+------------+------------+
| ASCII(' ') | ASCII('!') | ASCII('@') |
+------------+------------+------------+
|         32 |         33 |         64 |
+------------+------------+------------+
```
6. 制御文字

```sql
SELECT ASCII('\t'), ASCII('\n'), ASCII('\r');
```
```text
+-------------+-------------+-------------+
| ASCII('\t') | ASCII('\n') | ASCII('\r') |
+-------------+-------------+-------------+
|           9 |          10 |          13 |
+-------------+-------------+-------------+
```
7. 複数文字の文字列（最初の文字のみを返す）

```sql
SELECT ASCII('Hello'), ASCII('World123');
```
```text
+----------------+------------------+
| ASCII('Hello') | ASCII('World123') |
+----------------+------------------+
|             72 |               87 |
+----------------+------------------+
```
8. UTF-8マルチバイト文字

```sql
SELECT ASCII('ṭṛì'), ASCII('ḍḍumai');
```
```text
+---------------+------------------+
| ASCII('ṭṛì')  | ASCII('ḍḍumai') |
+---------------+------------------+
|           225 |              225 |
+---------------+------------------+
```
9. 数字と文字の混在

```sql
SELECT ASCII('9abc'), ASCII('0xyz');
```
```text
+---------------+---------------+
| ASCII('9abc') | ASCII('0xyz') |
+---------------+---------------+
|            57 |            48 |
+---------------+---------------+
```
