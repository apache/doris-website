---
{
  "title": "SYNC",
  "language": "ja",
  "description": "このステートメントは、非マスターFrontend（FE）ノードのメタデータを同期するために使用されます。Apache Dorisでは、マスターFEノードのみがメタデータを書き込むことができます、"
}
---
## Description

このステートメントは、非master Frontend (FE) ノードのメタデータを同期するために使用されます。Apache Dorisでは、master FEノードのみがメタデータを書き込むことができ、他のFEノードはメタデータの書き込み操作をmasterに転送します。masterがメタデータの書き込み操作を完了した後、非masterノードではメタデータの再生に短い遅延が生じる場合があります。このステートメントを使用してメタデータの強制同期を行うことができます。

## Syntax

```sql
SYNC;
```
## アクセス制御要件

任意のユーザーまたはロールがこの操作を実行できます。


## 例

メタデータを同期する：

    ```sql
    SYNC;
    ```
