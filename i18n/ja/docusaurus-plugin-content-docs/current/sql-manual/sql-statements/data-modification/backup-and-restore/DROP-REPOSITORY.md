---
{
  "title": "DROP REPOSITORY",
  "language": "ja",
  "description": "この文は作成されたリポジトリを削除するために使用されます。"
}
---
## 説明

このステートメントは、作成されたリポジトリを削除するために使用されます。

## 構文

```sql
DROP REPOSITORY <repo_name>;
```
## 必須パラメータ
**<repo_name>**
> リポジトリの一意の名前。

## アクセス制御要件

| 権限               | オブジェクト                         | 備考                                               |
|:-------------------|:-----------------------------|:----------------------------------------------------|
| ADMIN_PRIV         | クラスタ全体の管理権限 | rootまたはスーパーユーザーのみがリポジトリを作成できます  |


## 使用上の注意
- リポジトリを削除すると、Doris内でのマッピングのみが削除され、実際のリポジトリデータは削除されません。削除後、同じLOCATIONを指定することでリポジトリを再度マッピングできます。

## 例

example_repoという名前のリポジトリを削除する：

```sql
DROP REPOSITORY `example_repo`;
```
