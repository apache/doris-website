---
{
  "title": "VARCHAR",
  "description": "VARCHAR(M) 可変長文字列。Mは可変長文字列のバイト長を表します。Mの範囲は1-65533です。",
  "language": "ja"
}
---
## VARCHAR
### デスクリプション
VARCHAR(M)
可変長文字列。Mは可変長文字列のバイト長を表します。Mの範囲は1-65533です。

注意：可変長文字列はUTF-8エンコーディングで格納されるため、通常英語文字は1バイト、中国語文字は3バイトを占有します。

### keywords
VARCHAR
