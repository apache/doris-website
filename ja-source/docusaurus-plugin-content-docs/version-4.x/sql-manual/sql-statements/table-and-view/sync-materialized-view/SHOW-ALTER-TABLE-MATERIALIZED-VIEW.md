---
{
  "title": "SHOW ALTER TABLE MATERIALIZED VIEW",
  "description": "同期されたマテリアライズドビューのビルドタスクのステータスを確認します。",
  "language": "ja"
}
---
## 概要

同期マテリアライズドビューのビルドタスクのステータスを確認します。

同期マテリアライズドビューの作成は非同期操作であるため、マテリアライズドビューの作成タスクを送信した後、ユーザーはコマンドを通じて同期マテリアライズドビューのビルドのステータスを非同期で確認する必要があります。

## 構文

```sql
SHOW ALTER TABLE MATERIALIZED VIEW FROM <database>
```
## 必須パラメータ

**1. `<database>`**

> 同期マテリアライズドビューのベースTableが属するデータベース。

## 権限

このSQLコマンドを実行するユーザーは、少なくとも以下の権限を持つ必要があります：

| 権限       | オブジェクト | 注意事項                                                     |
| ---------- | ------------ | ------------------------------------------------------------ |
| ALTER_PRIV | Table        | 現在のマテリアライズドビューが属するTableに対するALTER_PRIV権限が必要 |

## 例

```sql
SHOW ALTER TABLE MATERIALIZED VIEW FROM doc_db;
```
