---
{
  "title": "CHAR | 文字列関数",
  "sidebar_label": "CHAR",
  "description": "CHAR関数は各引数を整数として解釈し、それらの整数コード値で表される文字から構成される文字列を返します。",
  "language": "ja"
}
---
# CHAR

## 説明

CHAR関数は各引数を整数として解釈し、それらの整数コード値によって表される文字で構成された文字列を返します。

## 構文

```sql
CHAR(<expr>[, <expr> ...] [USING <charset_name>])
```
## パラメータ

| Parameter | デスクリプション |
| ------------------ | ----------------------------------------- |
| `<expr>` | 文字に変換される整数のコード値。型: INT |

## Return Value

VARCHAR型を返します。引数の整数のコード値に対応する文字で構成された文字列です。

特殊なケース:
- 引数がNULLの場合、空文字列を返します
- 結果の文字列が指定された文字セットに対して不正な場合、NULLを返します
- 255より大きい引数はマルチバイト文字に変換されます。例えば、`CHAR(15049882)`は`CHAR(229, 164, 154)`と同等です

## Examples

1. 基本的な使用方法: ASCII文字生成

```sql
SELECT CHAR(68, 111, 114, 105, 115);
```
```text
+--------------------------------------+
| char('utf8', 68, 111, 114, 105, 115) |
+--------------------------------------+
| Doris                                |
+--------------------------------------+
```
2. マルチバイトUTF-8文字（中国語）

```sql
SELECT CHAR(15049882, 15179199, 14989469);
```
```text
+--------------------------------------------+
| char('utf8', 15049882, 15179199, 14989469) |
+--------------------------------------------+
| 多睿丝                                     |
+--------------------------------------------+
```
3. 不正な文字はNULLを返す

```sql
SELECT CHAR(255);
```
```text
+-------------------+
| char('utf8', 255) |
+-------------------+
| NULL              |
+-------------------+
```
4. NULL値の処理

```sql
SELECT CHAR(NULL);
```
```text
+------------+
| CHAR(NULL) |
+------------+
|            |
+------------+
```
