---
{
  "title": "POW",
  "description": "第1引数の値を第2引数の値で累乗した結果を返します。",
  "language": "ja"
}
---
## 説明

第1引数を第2引数の累乗にした値を返します。

## エイリアス

- POWER
- FPOW
- DPOW

## 構文

```sql
POW(<a>, <b>)
```
## パラメータ

| Parameter | デスクリプション |
|-----------|------------|
| `<a>`   | ベース   |
| `<b>`   | べき指数  |

## Return value

整数型または浮動小数点型を返します。

特殊なケース：

- a が `IS NULL` または b が `IS NULL` の場合、`NULL` を返します。
- `b = 0` かつ a が `IS NOT NULL` の場合、常に `1` を返します。

## Examples

```sql
select pow(2, 0);
```
```text
+-------------------------------------------+
| pow(cast(2 as DOUBLE), cast(0 as DOUBLE)) |
+-------------------------------------------+
|                                         1 |
+-------------------------------------------+
```
```sql
select pow(2, 10);
```
```text
+--------------------------------------------+
| pow(cast(2 as DOUBLE), cast(10 as DOUBLE)) |
+--------------------------------------------+
|                                       1024 |
+--------------------------------------------+
```
```sql
select pow(1.2, 2);
```
```text
+---------------------------------------------+
| pow(cast(1.2 as DOUBLE), cast(2 as DOUBLE)) |
+---------------------------------------------+
|                                        1.44 |
+---------------------------------------------+
```
```sql
select pow(1.2, 2.1);
```
```text
+-----------------------------------------------+
| pow(cast(1.2 as DOUBLE), cast(2.1 as DOUBLE)) |
+-----------------------------------------------+
|                            1.4664951016517147 |
+-----------------------------------------------+
```
```sql
select pow(2, null);
```
```text
+------------------------------+
| pow(cast(2 as DOUBLE), NULL) |
+------------------------------+
|                         NULL |
+------------------------------+
```
```sql
select pow(null, 2);
```
```text
+------------------------------+
| pow(NULL, cast(2 as DOUBLE)) |
+------------------------------+
|                         NULL |
+------------------------------+
```
