---
{
  "title": "MAP | Map関数",
  "sidebar_label": "MAP",
  "description": "特定の型のMAP<K, V>を、複数のキー・値ペアのグループを使用して構築します",
  "language": "ja"
}
---
# MAP

## 説明

複数のキーバリューペアのグループを使用して、特定の型の[`MAP<K, V>`](../../../basic-element/sql-data-types/semi-structured/MAP.md)を構築します

## 構文

```sql
MAP( <key1> , <value1> [, <key2>, <value2> ... ])
```
## パラメータ
### オプションパラメータ
- `<key1>` は複数の型をサポートし（[`MAP<K, V>`](../../../basic-element/sql-data-types/semi-structured/MAP.md) を参照）、マップのキーを構築します
- `<value1>` はマップの値を構築します
### 可変パラメータ
複数のキー・バリューパラメータのグループをサポートします

## 戻り値

複数のキー・バリューペアのグループから構築された特定の型 `MAP<K, V>` を返します

## 注意事項
1. パラメータの数は偶数でなければならず（0でも可）、そうでなければエラーが報告されます。
2. キーパラメータは重複して現れることがありますが、Doris は重複するキーを削除します。
3. キーは NULL にすることができ、複数の NULL キーは重複排除されます。

## 例
1. 通常のパラメータ

    ```sql
    select map(1, "100", 0.1, 2),map(1, "100", 0.1, 2)[1];
    ```
    ```text
    +-----------------------+--------------------------+
    | map(1, "100", 0.1, 2) | map(1, "100", 0.1, 2)[1] |
    +-----------------------+--------------------------+
    | {1.0:"100", 0.1:"2"}  | 100                      |
    +-----------------------+--------------------------+
    ```
2. パラメータなしの場合

    ```sql
    select map();
    ```
    ```text
    +-------+
    | map() |
    +-------+
    | {}    |
    +-------+
    ```
3. NULL パラメータ

    ```sql
    select map(null, 2, 3, null);
    ```
    ```text
    +-----------------------+
    | map(null, 2, 3, null) |
    +-----------------------+
    | {null:2, 3:null}      |
    +-----------------------+
    ```
4. 重複するキーがある場合、重複は除去されます

    ```sql
    select map(1, 2, 2, 11, 1, 3, null, "null 1", null, "null 2");
    ```
    ```text
    +--------------------------------------------------------+
    | map(1, 2, 2, 11, 1, 3, null, "null 1", null, "null 2") |
    +--------------------------------------------------------+
    | {2:"11", 1:"3", null:"null 2"}                         |
    +--------------------------------------------------------+
    ```
