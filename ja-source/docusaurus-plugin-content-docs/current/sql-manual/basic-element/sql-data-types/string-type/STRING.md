---
{
  "title": "文字列",
  "language": "ja",
  "description": "STRING (M) 可変長文字列。デフォルトサポートは1048576バイト（1M）、2147483643バイト（2G）まで調整可能。"
}
---
## STRING
### 説明
STRING (M)
可変長文字列。デフォルトサポートは1048576バイト（1M）で、最大2147483643バイト（2G）まで調整可能です。また、Stringタイプの長さはbeのstring_type_length_soft_limit_bytes（文字列タイプ長のソフト制限）設定によっても制限されます。Stringタイプは値カラムでのみ使用でき、キーカラムやパーティションおよびバケットカラムでは使用できません。

注意：可変長文字列はUTF-8エンコーディングで格納されるため、通常英字は1バイト、中国語文字は3バイトを占有します。

### キーワード
STRING
