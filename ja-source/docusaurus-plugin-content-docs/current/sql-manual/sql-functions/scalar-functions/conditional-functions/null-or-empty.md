---
{
  "title": "NULL_OR_EMPTY",
  "language": "ja",
  "description": "nullorempty関数は、与えられた値がNULLでなく、かつ空でないかを判定するために使用されます。入力値がNULLでも空でもない場合、"
}
---
## 説明

`null_or_empty`関数は、与えられた値がNULLでなく、かつ空でないかどうかを判定するために使用されます。入力値がNULLでも空でもない場合、trueを返します。そうでない場合はfalseを返します。

## 構文

```sql
NULL_OR_EMPTY (<str>)
```
## パラメータ
- `<str>`: String型、NULLまたは空文字列かどうかをチェックする文字列。

## 戻り値
文字列が空文字列またはNULLの場合はtrueを返す。そうでなければfalseを返す。

## 例
1. 例1

    ```sql
    select null_or_empty(null), null_or_empty("");, null_or_empty(" ");
    ```
    ```text
    +---------------------+-------------------+--------------------+
    | null_or_empty(null) | null_or_empty("") | null_or_empty(" ") |
    +---------------------+-------------------+--------------------+
    |                   1 |                 1 |                  0 |
    +---------------------+-------------------+--------------------+
    ```
