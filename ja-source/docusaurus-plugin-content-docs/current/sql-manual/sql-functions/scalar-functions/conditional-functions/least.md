---
{
  "title": "LEAST",
  "language": "ja",
  "description": "複数の式を比較し、その中で最小の値を返します。引数のいずれかがNULLの場合、NULLを返します。"
}
---
## 説明

複数の式を比較し、その中の最小値を返します。引数のいずれかが`NULL`の場合、`NULL`を返します。

## 構文

```sql
LEAST(<expr> [, ...])
```
## パラメータ
### 必須パラメータ
- `<expr>`: `TINYINT`、`SMALLINT`、`INT`、`BIGINT`、`LARGEINT`、`FLOAT`、`DOUBLE`、`STRING`、`DATETIME`、および`DECIMAL`型をサポートします。
### オプションパラメータ
- 複数の引数をサポートします。

## 戻り値
- 指定された式の中で最小の値を返します。
- いずれかの引数が`NULL`の場合、`NULL`を返します。

## 使用上の注意
1. 同じ型の引数を渡すことを推奨します。引数の型が異なる場合、関数は同じ型に変換を試みます。変換規則については、以下を参照してください：[Type Conversion](../../../basic-element/sql-data-types/conversion/overview.md)
2. いずれかの引数がNULLの場合、結果はNULLになります。


## 例
1. 例1

    ```sql
    SELECT LEAST(-1, 0, 5, 8);
    ```
    ```text
    +--------------------+
    | LEAST(-1, 0, 5, 8) |
    +--------------------+
    |                 -1 |
    +--------------------+
    ```
2. NULL引数

    ```sql
    SELECT LEAST(-1, 0, 5, NULL);
    ```
    ```text
    +-----------------------+
    | LEAST(-1, 0, 5, NULL) |
    +-----------------------+
    |                  NULL |
    +-----------------------+
    ```
3. 型変換

    ```sql
    SELECT LEAST(6, 9.29, 7);
    ```
    ```text
    +-------------------+
    | LEAST(6, 9.29, 7) |
    +-------------------+
    |              6.00 |
    +-------------------+
    ```
> 最初の引数「6」はDecimal型に変換されます。
4. Date型

    ```sql
    SELECT LEAST('2022-02-26 20:02:11', '2020-01-23 20:02:11', '2020-06-22 20:02:11');
    ```
    ```text
    +----------------------------------------------------------------------------+
    | LEAST('2022-02-26 20:02:11', '2020-01-23 20:02:11', '2020-06-22 20:02:11') |
    +----------------------------------------------------------------------------+
    | 2020-01-23 20:02:11                                                        |
    +----------------------------------------------------------------------------+
    ```
