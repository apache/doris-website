---
{
  "title": "MASK_FIRST_N",
  "language": "ja",
  "description": "MASKFIRSTN関数は文字列の最初のNバイトをマスクするために使用されます。大文字をXに、小文字をxに置き換えます。"
}
---
## 説明

MASK_FIRST_N関数は、文字列の最初のNバイトをマスクするために使用されます。最初のNバイト内の大文字を`X`、小文字を`x`、数字を`n`に置き換えます。

## 構文

```sql
MASK_FIRST_N(<str>[, <n>])
```
## パラメータ

| パラメータ | 説明 |
| ------- | ----------------------------------------- |
| `<str>` | マスクする文字列。型: VARCHAR |
| `<n>` | マスクする最初のNバイト数（オプション、デフォルトは文字列全体）。型: INT |

## 戻り値

VARCHAR型を返します。最初のNバイトがマスクされた文字列です。

特殊なケース:
- いずれかのパラメータがNULLの場合、NULLを返します
- 英数字以外の文字は変更されません
- `<n>` が文字列長より大きい場合、文字列全体をマスクします

## 例

1. 基本的な使用法: 最初の4バイトをマスク

```sql
SELECT mask_first_n('1234-5678', 4);
```
```text
+------------------------------+
| mask_first_n('1234-5678', 4) |
+------------------------------+
| nnnn-5678                    |
+------------------------------+
```
2. nを指定しない場合（文字列全体をマスクする）

```sql
SELECT mask_first_n('abc123');
```
```text
+-----------------------+
| mask_first_n('abc123') |
+-----------------------+
| xxxnnn                |
+-----------------------+
```
3. nが文字列長を超える場合

```sql
SELECT mask_first_n('Hello', 100);
```
```text
+----------------------------+
| mask_first_n('Hello', 100) |
+----------------------------+
| Xxxxx                      |
+----------------------------+
```
4. NULL値の処理

```sql
SELECT mask_first_n(NULL, 5);
```
```text
+-----------------------+
| mask_first_n(NULL, 5) |
+-----------------------+
| NULL                  |
+-----------------------+
```
5. nが0の場合（文字をマスクしない）

```sql
SELECT mask_first_n('Hello123', 0);
```
```text
+-----------------------------+
| mask_first_n('Hello123', 0) |
+-----------------------------+
| Hello123                    |
+-----------------------------+
```
6. nが文字列長より大きい場合（文字列全体をマスクする）

```sql
SELECT mask_first_n('Test', 100);
```
```text
+---------------------------+
| mask_first_n('Test', 100) |
+---------------------------+
| Xxxx                      |
+---------------------------+
```
7. メールアドレスのプレフィックスをマスクする

```sql
SELECT mask_first_n('user@example.com', 6);
```
```text
+-------------------------------------+
| mask_first_n('user@example.com', 6) |
+-------------------------------------+
| xxxx@xxample.com                    |
+-------------------------------------+
```
8. 電話番号の最初の3桁をマスクする

```sql
SELECT mask_first_n('13812345678', 3);
```
```text
+--------------------------------+
| mask_first_n('13812345678', 3) |
+--------------------------------+
| nnn12345678                    |
+--------------------------------+
```
9. 英字、数字、特殊文字の混在

```sql
SELECT mask_first_n('Abc-123-XYZ', 7);
```
```text
+---------------------------------+
| mask_first_n('Abc-123-XYZ', 7)  |
+---------------------------------+
| Xxx-nnn-XYZ                     |
+---------------------------------+
```
10. UTF-8文字処理（バイト単位でのマスク）

```sql
SELECT mask_first_n('ṭṛWorld123', 7);
```
```text
+-----------------------------------+
| mask_first_n('ṭṛWorld123', 7)     |
+-----------------------------------+
| ṭṛXorld123                        |
+-----------------------------------+
```
```sql
SELECT mask_first_n('eeeéèêëìí1234');
```
```text
+-------------------------------------+
| mask_first_n('eeeéèêëìí1234')       |
+-------------------------------------+
| xxxéèêëìínnnn                       |
+-------------------------------------+
```
