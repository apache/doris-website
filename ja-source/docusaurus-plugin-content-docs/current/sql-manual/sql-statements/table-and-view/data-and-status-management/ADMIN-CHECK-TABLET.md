---
{
  "title": "管理者チェック用タブレット",
  "language": "ja",
  "description": "このステートメントは、タブレットのセットに対して指定されたチェック操作を実行するために使用されます。"
}
---
## 説明

このステートメントは、タブレットのセットに対して指定されたチェック操作を実行するために使用されます。

## 構文

```sql
ADMIN CHECK TABLET ( <tablet_id> [,...] ) PROPERTIES("type" = "<type_value>")
```
## 必須パラメータ

**1. `<tablet_id>`**

指定されたチェック操作が実行されるタブレットのID。


## オプションパラメータ

**1. `<type_value>`**

現在、consistencyのみがサポートされています。

- consistency:

  タブレットのレプリカデータ整合性をチェックします。このコマンドは非同期であり、送信後にDorisが対応するタブレットの整合性チェックジョブの実行を開始することを意味します。

## 戻り値

ステートメントの最終結果は、`SHOW PROC "/cluster_health/tablet_health";`の結果のInconsistentNum列に反映されます。

| Column          | DataType | Note                     |
|-----------------|----------|--------------------------|
| InconsistentNum | Int      | 不整合があるタブレットの数 |


## アクセス制御要件

このSQLコマンドを実行するユーザーは、少なくとも以下の権限を持つ必要があります：

| Privilege  | Object   | Notes                                                                                        |
|:-----------|:---------|:---------------------------------------------------------------------------------------------|
| Admin_priv | Database | テーブル、パーティション、システムレベルコマンドの管理を含む、データベースでの管理操作を実行するために必要。 |

## 例
- 指定されたタブレットのセットでレプリカデータ整合性チェックを実行する

  ```sql
  admin check tablet (10000, 10001) PROPERTIES("type" = "consistency");
  ```
