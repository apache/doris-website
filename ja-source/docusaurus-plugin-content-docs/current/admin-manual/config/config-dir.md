---
{
  "title": "設定ディレクトリ",
  "language": "ja",
  "description": "FEとBEの設定ファイルディレクトリはconf/です。デフォルトのfe.conf、be.confおよびその他のファイルの格納に加えて、"
}
---
# Config Dir

FEとBEの設定ファイルディレクトリは`conf/`です。このディレクトリは、デフォルトのfe.conf、be.confなどのファイルを格納することに加えて、共通の設定ファイル格納ディレクトリとしても使用されます。

ユーザーはいくつかの設定ファイルをその中に格納することができ、システムが自動的にそれらを読み取ります。

## hdfs-site.xmlとhive-site.xml

Dorisの一部の機能では、HDFS上のデータにアクセスしたり、Hive metastoreにアクセスする必要があります。

機能の対応するステートメントで、さまざまなHDFS/Hiveパラメータを手動で記入することができます。

しかし、これらのパラメータは非常に多く、すべてを手動で記入すると非常に面倒です。

そのため、ユーザーはHDFSまたはHiveの設定ファイルhdfs-site.xml/hive-site.xmlを直接`conf/`ディレクトリに配置することができます。Dorisはこれらの設定ファイルを自動的に読み取ります。

ユーザーがコマンドで記入する設定は、設定ファイル内の設定項目を上書きします。

このようにして、ユーザーは少量の設定を記入するだけで、HDFS/Hiveへのアクセスを完了することができます。
