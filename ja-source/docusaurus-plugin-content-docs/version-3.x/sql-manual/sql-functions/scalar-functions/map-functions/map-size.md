---
{
  "title": "MAP_SIZE",
  "description": "Map内の要素数を数える",
  "language": "ja"
}
---
## 説明

Map内の要素数を数える

## 構文

```sql
MAP_SIZE(<map>)
```
## パラメータ

| Parameter | デスクリプション |
| -- | -- |
| `<map>` | 入力マップコンテンツ |

## Return Value

Mapの要素数を返します

## Example

```sql
select map_size(map()),map_size(map(1, "100", 0.1, 2));
```
```text
+-----------------+-------------------------------------------------------------------------------------------------+
| map_size(map()) | map_size(map(cast(1 as DECIMALV3(2, 1)), '100', cast(0.1 as DECIMALV3(2, 1)), cast(2 as TEXT))) |
+-----------------+-------------------------------------------------------------------------------------------------+
|               0 |                                                                                               2 |
+-----------------+-------------------------------------------------------------------------------------------------+
```
