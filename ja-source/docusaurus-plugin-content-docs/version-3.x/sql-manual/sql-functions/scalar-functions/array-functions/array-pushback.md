---
{
  "title": "ARRAY_PUSHBACK",
  "description": "配列の末尾に値を追加する",
  "language": "ja"
}
---
## 説明

配列の末尾に値を追加する

## 構文

```sql
ARRAY_PUSHBACK(<arr>, <value>)
```
## パラメータ

| パラメータ | 説明 |
|--|---|
| `<arr>` | 対応する配列 |
| `<value>` | 追加される値 |

## 戻り値

値を追加した後の配列を返します

## 例

```sql
SELECT ARRAY_PUSHBACK([1, 2], 3),ARRAY_PUSHBACK([3, 4], 6);
```
```text
+---------------------------+---------------------------+
| array_pushback([1, 2], 3) | array_pushback([3, 4], 6) |
+---------------------------+---------------------------+
| [1, 2, 3]                 | [3, 4, 6]                 |
+---------------------------+---------------------------+
```
