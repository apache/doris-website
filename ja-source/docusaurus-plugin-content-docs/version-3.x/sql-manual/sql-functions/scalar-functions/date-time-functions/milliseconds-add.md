---
{
  "title": "MILLISECONDS_ADD",
  "description": "指定されたミリ秒数をdatetime値に加算し、新しいdatetime値を返します。",
  "language": "ja"
}
---
## 説明

datetime値に指定されたミリ秒数を加算し、新しいdatetime値を返します。

## 構文

```sql
MILLISECONDS_ADD(<basetime>, <delta>)
```
## パラメータ

| パラメータ | 説明                                             |
|-----------|--------------------------------------------------|
| `<basetime>`  | DATETIMEV2型の入力日時値    |
| `<delta>`     | 追加するミリ秒数、INT型; 1秒 = 1,000ミリ秒 = 1,000,000マイクロ秒 |

## 戻り値

DATETIMEV2型の値を返します。これは入力日時に指定されたミリ秒数を追加した後の時間値を表します。戻り値の精度は入力パラメータbasetimeと同じです。

## 例

```sql
SELECT MILLISECONDS_ADD('2023-09-08 16:02:08.435123', 1);
```
```text
+--------------------------------------------------------------------------+
| milliseconds_add(cast('2023-09-08 16:02:08.435123' as DATETIMEV2(6)), 1) |
+--------------------------------------------------------------------------+
| 2023-09-08 16:02:08.436123                                               |
+--------------------------------------------------------------------------+
```
**注意:**
- この例では、1ミリ秒を加算した後、時刻が.435123から.436123に増加します。
- 1ミリ秒は1000マイクロ秒に等しいです。
- この関数の結果は入力時刻の精度に依存します。この例では小数点以下6桁の精度を使用しています。
