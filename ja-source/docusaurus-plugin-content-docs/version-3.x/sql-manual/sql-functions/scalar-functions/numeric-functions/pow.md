---
{
  "title": "POW",
  "description": "第一引数を第二引数の累乗した値を返します。",
  "language": "ja"
}
---
## 説明

第1引数を第2引数で累乗した値を返します。

## エイリアス

- POWER
- FPOW
- DPOW

## 構文

```sql
POW(<a>, <b>)
```
## パラメータ

| Parameter | デスクリプション |
|-----------|------------|
| `<a>`   | 基数   |
| `<b>`   | 指数  |

## 戻り値

整数型または浮動小数点型を返します。

特別なケース:

- a `IS NULL` または b `IS NULL` の場合、`NULL` を返します。
- `b = 0` かつ a `IS NOT NULL` の場合、常に `1` を返します。

## 例

```sql
select pow(2, 0);
```
```text
+-------------------------------------------+
| pow(cast(2 as DOUBLE), cast(0 as DOUBLE)) |
+-------------------------------------------+
|                                         1 |
+-------------------------------------------+
```
```sql
select pow(2, 10);
```
```text
+--------------------------------------------+
| pow(cast(2 as DOUBLE), cast(10 as DOUBLE)) |
+--------------------------------------------+
|                                       1024 |
+--------------------------------------------+
```
```sql
select pow(1.2, 2);
```
```text
+---------------------------------------------+
| pow(cast(1.2 as DOUBLE), cast(2 as DOUBLE)) |
+---------------------------------------------+
|                                        1.44 |
+---------------------------------------------+
```
```sql
select pow(1.2, 2.1);
```
```text
+-----------------------------------------------+
| pow(cast(1.2 as DOUBLE), cast(2.1 as DOUBLE)) |
+-----------------------------------------------+
|                            1.4664951016517147 |
+-----------------------------------------------+
```
```sql
select pow(2, null);
```
```text
+------------------------------+
| pow(cast(2 as DOUBLE), NULL) |
+------------------------------+
|                         NULL |
+------------------------------+
```
```sql
select pow(null, 2);
```
```text
+------------------------------+
| pow(NULL, cast(2 as DOUBLE)) |
+------------------------------+
|                         NULL |
+------------------------------+
```
