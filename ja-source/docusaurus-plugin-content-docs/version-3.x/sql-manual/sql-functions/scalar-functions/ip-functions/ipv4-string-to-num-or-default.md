---
{
  "title": "IPV4_STRING_TO_NUM_OR_DEFAULT",
  "description": "IPv4アドレスをA.B.C.D形式（10進数をドット区切りした形式）で含む文字列を受け取ります。",
  "language": "ja"
}
---
## デスクリプション
A.B.C.D形式（10進数をドット区切りした形式）のIPv4アドレスを含む文字列を受け取ります。対応するIPv4アドレスをビッグエンディアン形式で表すBIGINT数値を返します。

## Syntax

```sql
IPV4_STRING_TO_NUM_OR_DEFAULT(<ipv4_string>)
```
## パラメータ
| Parameter | デスクリプション                                      |
|-----------|--------------------------------------------------|
| `<ipv4_string>`      | 'A.B.C.D'のようなipv4の文字列型  |


## Return Value
ビッグエンディアンで対応するIPv4アドレスを表すBIGINT数値を返します。
- 入力文字列が有効なIPv4アドレスでない場合や`NULL`の場合、`0`が返されます。

## Example

```sql
select ipv4_string_to_num_or_default('192.168.0.1');
```
```text
+----------------------------------------------+ 
| ipv4_string_to_num_or_default('192.168.0.1') | 
+----------------------------------------------+ 
| 3232235521                                   | 
+----------------------------------------------+ 
```
```sql
select str, ipv4_string_to_num_or_default(str) from ipv4_str; 
```
```text
+-----------------+------------------------------------+ 
|str              | ipv4_string_to_num_or_default(str) | 
+-----------------+------------------------------------+ 
| 0.0.0.0         | 0                                  | 
| 127.0.0.1       | 2130706433                         | 
| 255.255.255.255 | 4294967295                         | 
| invalid         | 0                                  | 
+-----------------+------------------------------------+ 
```
