---
{
  "title": "SHOW DATABASE ID",
  "description": "このステートメントは、データベースIDに基づいて対応するデータベース名を検索するために使用されます（管理者のみが使用）",
  "language": "ja"
}
---
### 説明

このステートメントは、データベースID に基づいて対応するデータベース名を検索するために使用されます（管理者のみが使用）

## 構文

```sql
SHOW DATABASE <database_id>
```
## 必須パラメータ

** 1. `<database_id>`**
>  データベースの対応するID番号

## 戻り値

| カラム | 説明 |
|:---------|:-----------|
| DbName |  データベース名|

## 権限制御

このSQLコマンドを実行するユーザーは、少なくとも以下の権限を持つ必要があります：

| 権限         | オブジェクト   | 注意事項            |
|:-----------|:-----|:--------------|
| ADMIN_PRIV | クラスタ全体 | クラスタ全体の管理権限が必要 |

## 例

- データベースIDに応じて対応するデータベース名を検索

    ```sql
    SHOW DATABASE 10396;
    ```
    ```text
    +------------+
    | DbName     |
    +------------+
    | example_db |
    +------------+
    ```
