---
{
  "title": "LN",
  "language": "ja",
  "description": "xの自然対数（底e）を返します。"
}
---
## 説明

`x`の自然対数（底`e`）を返します。

## エイリアス

- DLOG1

## 構文

```sql
LN(<x>)
```
## パラメータ

| パラメータ | 説明 |
|-----------|------------|
| `<x>`   | 真数は0より大きい値である必要があります |

## 戻り値

浮動小数点数を返します。特殊なケース:

- x が NULL の場合、NULL を返します

## 例

```sql
select ln(1);
```
```text
+-----------------------+
| ln(cast(1 as DOUBLE)) |
+-----------------------+
|                   0.0 |
+-----------------------+
```
```sql
select ln(e());
```
```text
+---------+
| ln(e()) |
+---------+
|     1.0 |
+---------+
```
```sql
select ln(10);
```
```text
+------------------------+
| ln(cast(10 as DOUBLE)) |
+------------------------+
|      2.302585092994046 |
+------------------------+
```
