---
{
  "title": "UNHEX",
  "language": "ja",
  "description": "unhex関数は、16進文字列を元の文字列に変換するために使用されます。この関数は、2つの16進文字を1バイトに変換します。"
}
---
## 説明

`unhex`関数は、16進数文字列を元の文字列に戻すために使用されます。2つの16進数文字ごとに1バイトに変換します。無効な値がパラメータとして渡された場合、空文字列を返します。

## 構文

```sql
UNHEX(<str>)
```
## パラメータ

| Parameter | Description |
| -- | -- |
| `<str>` | 16進文字列 |

## 戻り値

入力文字列の長さが0または奇数の場合、`unhex`関数は空文字列を返します。
文字列に[0-9]、[a-f]、または[A-F]以外の文字が含まれている場合、`unhex`関数は空文字列を返します。
その他の場合、2文字ごとに16進表現に変換され、文字列として連結されて出力されます。

## 例

```sql
select unhex('@');
```
```text
+------------+
| unhex('@') |
+------------+
|            |
+------------+
```
```sql
select unhex('41');
```
```text
+-------------+
| unhex('41') |
+-------------+
| A           |
+-------------+
```
```sql
select unhex('4142');
```
```text
+---------------+
| unhex('4142') |
+---------------+
| AB            |
+---------------+
```
