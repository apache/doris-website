---
{
  "title": "MAP_VALUES",
  "language": "ja",
  "description": "指定されたmapの値を対応する型のARRAYに抽出する"
}
---
## 説明

指定された`map`の値を、対応する型の`ARRAY`に抽出します

## 構文

```sql
MAP_VALUES(<map>)
```
## パラメータ

| パラメータ | 説明 |
| -- | -- |
| `<map>` | 入力するマップのコンテンツ |

## 戻り値

与えられた`map`の値を対応する型の`ARRAY`に抽出する

## 例

```sql
select map_values(map()),map_values(map(1, "100", 0.1, 2));
```
```text
+-------------------+---------------------------------------------------------------------------------------------------+
| map_values(map()) | map_values(map(cast(1 as DECIMALV3(2, 1)), '100', cast(0.1 as DECIMALV3(2, 1)), cast(2 as TEXT))) |
+-------------------+---------------------------------------------------------------------------------------------------+
| []                | ["100", "2"]                                                                                      |
+-------------------+---------------------------------------------------------------------------------------------------+
```
