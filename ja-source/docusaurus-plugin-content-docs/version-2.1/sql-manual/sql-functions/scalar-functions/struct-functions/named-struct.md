---
{
  "title": "NAMED_STRUCT",
  "language": "ja",
  "description": "与えられた文字列と値に基づいてstructを構築して返します。注意事項："
}
---
## 説明

与えられた文字列と値に基づいて構造体を構築して返します。注意事項：

- パラメータ数は非ゼロの偶数である必要があります。奇数インデックスの要素はフィールドの名前で、定数文字列である必要があります。偶数インデックスの要素はフィールドの値で、複数の列または定数のいずれかを指定できます。

## 構文

```sql
NAMED_STRUCT( <field_name> , <filed_value> [ , <field_name> , <filed_value> ... ] )
```
## パラメータ

| パラメータ | 説明 |
| -- | -- |
| `<field_name>` | 構造体を構築する際の奇数インデックスの要素はフィールド名であり、定数文字列である必要があります |
| `<filed_value>` | 構造体を構築する際の偶数インデックスの要素はフィールド値を表し、複数の列または定数のいずれかを指定できます |

## 戻り値

指定された文字列と値に基づいて構造体を構築して返します。

## 例

```sql
select named_struct('f1', 1, 'f2', 'a', 'f3', "abc"),named_struct('a', null, 'b', "v");
```
```text
+-----------------------------------------------+-----------------------------------+
| named_struct('f1', 1, 'f2', 'a', 'f3', 'abc') | named_struct('a', NULL, 'b', 'v') |
+-----------------------------------------------+-----------------------------------+
| {"f1":1, "f2":"a", "f3":"abc"}                | {"a":null, "b":"v"}               |
+-----------------------------------------------+-----------------------------------+
```
