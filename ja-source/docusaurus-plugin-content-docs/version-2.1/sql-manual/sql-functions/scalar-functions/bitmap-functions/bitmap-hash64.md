---
{
  "title": "BITMAP_HASH64",
  "language": "ja",
  "description": "任意の入力タイプの64ビットhash値を計算し、そのhash値を含むBitmapを返します。"
}
---
## 説明

任意の入力タイプの64ビットハッシュ値を計算し、そのハッシュ値を含むBitmapを返します。

## 構文

```sql
BITMAP_HASH64(<expr>)
```
## パラメータ

| パラメータ | 説明           |
|-----------|-----------------------|
| `<expr>`  | 任意の値またはフィールド式 |

## 戻り値

パラメータ `<expr>` の64ビットハッシュ値を含むBitmapを返します。

## 例

値の64ビットハッシュを計算するには、次のように使用できます：

```sql
select bitmap_to_string(bitmap_hash64('hello'));
```
結果は以下のようになります：

```text
+------------------------------------------+
| bitmap_to_string(bitmap_hash64('hello')) |
+------------------------------------------+
| 15231136565543391023                     |
+------------------------------------------+
```
